// Real Etsy reviews from slidellstitches shop.
// Replace this list with the actual reviews Stephanie shares.
// Each review should be a verified Etsy buyer.

export type Testimonial = {
  rating: number;
  text: string;
  name: string;
  date: string;
  product?: string;
};

export const testimonials: Testimonial[] = [
  // PLACEHOLDER — replace with real Etsy reviews
];
